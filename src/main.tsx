import React from "react";
import ReactDOM from "react-dom/client";
import NoticeFlow from "./NoticeFlow";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <NoticeFlow />
  </React.StrictMode>
);
